Hgal TODO List:

实现条件编译--ok,支持if,elseif,else,end，找到了一个简单有效的办法解决了此问题，耶耶耶耶耶
	
1、临时变量vec3如果使用normalize函数其必须使用寄存器的前3位 --- 临时解决办法是只用前3位
	
2、找个办法实现临时变量不需要在头部初始化的过程。（一个临时变量第一次赋值，给每个mask都赋值？,这个方法不好很可能会导致有问题，目前先不使用这个方法，看是否有问题）

3、判断函数中参数是否合法

4、实现纯数字数组
	
