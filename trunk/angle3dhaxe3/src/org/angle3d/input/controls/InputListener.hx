package org.angle3d.input.controls
{

	/**
	 * A generic public interface for input listeners, the {@link AnalogListener} and
	 * {@link ActionListener} public interfaces extend this public interface.
	 *
	 * @author Kirill Vainer
	 */
	public interface InputListener
	{
	}
}

